import { RefactorNumberPipe } from './refactor-number.pipe';

describe('RefactorNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new RefactorNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
